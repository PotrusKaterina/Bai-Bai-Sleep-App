import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    input: {
        width: 280,
        borderRadius: 2,
        borderColor: '#a6a6a6',
        borderWidth: 0.8,
        margin: 10,
    },
});